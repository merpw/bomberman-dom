import { FC } from "react";

const Game: FC = () => {
  return (
    <div className={"h-full flex flex-col"}>
      <p className={"grow border border-primary"}>Add game here</p>
    </div>
  );
};

export default Game;
