import one_up from "./1_up.svg";

type PlayerAssets = {
  up: string;
  down: string;
  left: string;
  right: string;
};

const PlayerAssets: PlayerAssets[] = [
  {
    up: one_up,
    down: one_up,
    left: one_up,
    right: one_up,
  },
  {
    up: one_up,
    down: one_up,
    left: one_up,
    right: one_up,
  },
];
export default PlayerAssets;
